# Agent Guidelines — transcribe-ui

## Project overview

NiceGUI-based web frontend for Sunet Scribe (transcription service). Requires Python ≥ 3.13. Package manager: `uv` (see `uv.lock`).

## Architecture

- **Framework**: NiceGUI (Python-based web UI built on Quasar/Vue)
- **Entry point**: `main.py` — registers `@ui.page("/")` and `/logout`, handles OIDC auth callback and encryption-password bootstrap
- **Pages**: `pages/` — `home.py`, `admin/` (package, one module per admin page: `analytics.py`, `announcements.py`, `customers.py`, `groups.py`, `health.py`, `rules.py`, `shared.py`, `users.py`), `srt.py`, `user.py`, `status.py`. Each top-level page module exports a `create()` function called from `main.py`; `pages/admin/__init__.py` imports the admin submodules for their `@ui.page` side effects and re-exports `create` from `groups.py` for `/admin` itself.
- **Utilities**: `utils/` — split by concern:
  - `helpers.py` — `storage_encrypt`/`storage_decrypt` (AES-256-GCM), filename sanitisation, encryption-password set/verify/reset, customer/realm CRUD
  - `token.py` — `get_auth_header`, `token_refresh`, `get_user_info`, `get_admin_status`, `get_bofh_status`
  - `common.py` — `page_init()` and shared UI scaffolding
  - `settings.py` — pydantic `BaseSettings` loaded from `.env` via `get_settings()` (lru-cached)
  - `caption.py` (`SRTCaption`), `video.py`, `undo_redo.py`, `customer.py`, `group.py`, `crypto.py`, `styles.py`
  - `markdown_blocks.py` — an answer read as the blocks and runs it is made of, shared by the exports below
  - `docx_export.py` / `omml.py` — the Word export of an assistant's answer, and the LaTeX-to-OMML conversion that keeps its formulae editable (see “Analysing a transcription” below)
  - `latex_export.py` — the same answer as a `.tex` document
  - `speech_timeline.py` / `speech_timeline.js` — the speech strip under the video (see below)
  - `recorder.py` — the microphone recorder in the upload dialog (see “Recording into the upload dialog” below)
  - The SRT/transcript editor is split across several files rather than one monolith — see "Editor split" below.
- **DB / analytics**: `db/analytics.py` — async `httpx` calls to backend API
- **Static assets**: `static/` — logos, favicon
- **Tests**: `tests/` — pytest with `nicegui.testing.user_plugin`; run with `uv run pytest` (or `.venv/bin/python -m pytest`)
- **Container**: `Dockerfile` at repo root

### Editor split (`utils/srt*.py`, `utils/transcript_editor.*`)

`pages/srt.py` opens one editor regardless of `data_format` — `TranscriptEditor` (`utils/transcript_editor.py`), a single `contenteditable` (Vue component `transcript_editor.js`) — backed by one `SRTEditor` (`utils/srt.py`) so saving, exporting, and word/review data work identically for both a transcription and a set of subtitles. `SRTEditor.render_override` points at `TranscriptEditor.refresh`, which is what `refresh_display()` calls; `SRTEditor.on_select` points at `TranscriptEditor.scroll_to`, which is what `select_caption()` (search, autoscroll) calls to bring a caption into view. The component takes a `subtitleMode` prop (`self.editor.data_format == "srt"`, set once in `TranscriptEditor.build()`) that swaps the speaker margin for a character-count guideline, one row per line (`caption_line_counts()`), each row lined up with its text line the same way the base rule lines the speaker up with the text. Those counts are redrawn as the caption is typed into, from the DOM text itself (`computeLineCounts()`, mirroring `caption_line_counts()` — same guideline, same tooltip wording, `characterLimit`/`maxSubtitleLines` sent down as props rather than duplicated as constants) into a `liveCounts` map keyed by block id, since `set_text()` deliberately does not re-render the block being typed into (it would move the caret) and so nothing else would keep the counts current; a real render (split/merge/undo) clears the map so the server's own answer takes back over — the margin is a column (`.transcript-subtitle-mode .transcript-gutter`, `flex-direction: column`) rather than the base rule's row, the caption's index heading it at the same height as the cell's timing row (`.transcript-subtitle-index`, a flex box of `height: 1.5rem` with the figure centred in it, matching `.transcript-action`'s own height — see below — rather than the shorter plain text beside it, since that is what actually decides the timing row's rendered height; centring rather than a tall `line-height` on a small figure, which half-leading pushed to the top of its box and read as the index sitting above the timestamp) with the per-line counts stacked below (`.transcript-subtitle-counts`), a `gap` matching `.transcript-subtitle-time`'s own `margin-bottom` keeping the first count aligned with the text's first line the same way the index lines up with the timing above it. The index names the caption itself, in the same column the counts are in but its own row, not folded into the first count row the way it once was. The timing itself sits in the cell, over the text it times rather than over the margin's counts, as two plain `<input>`s (`.transcript-time-input`) edited directly — no dialog, in either mode; a typed value is read back by `parse_time_label()` and applied via `retime()`/`apply_time()`, and a value that does not parse (or an end before the start) is refused and the input reverted, forced by marking the block dirty the same way a text edit does so Vue's own diff cannot skip a prop that ends up unchanged. A transcription's own `.transcript-time` uses the same input pair, just outside `.transcript-subtitle-content`'s flex wrapper (stacking the timing above the text, which only subtitleMode needs — a transcription's own `.transcript-time` and `.transcript-text` sit as plain siblings instead). subtitleMode also turns on per-caption split/merge/note/add/delete actions (`.transcript-cell-actions`), riding the same timing row rather than sitting beside the text below — quiet (opacity 0) until the caption is hovered or a reader tabs into one, still in flow so hidden it reserves its own width and height and the row does not reflow as they fade in and out. The row is a three-column grid (`1fr auto 1fr`) with the timing wrapped in `.transcript-subtitle-timing` as the first column and the actions as the middle one, which is what lands them on the row's *true* centre; equal auto margins on a flex item only centred them in whatever space the timing left over, noticeably right of centre, and the empty third column exists purely to balance the first (`splitAt()`, mouse equivalent of Ctrl/Cmd+Enter — prefers the caret when it is already in that caption, otherwise the middle of its text; merge reuses the existing `mergeblock`/`direction: "next"` event Delete-at-the-end already emits, no new server-side method) — a transcription has speakers and no length limit; a subtitle has the opposite. The music note (`insertNote()`, `♪` — a caption of song or of music under the picture is written with one, and no keyboard offers the character) is the one action in that row that is *not* structural: it is typed into the caption at the caret and carried to the server by the same `onInput()` a keystroke goes through, so it is marked as the reader's own and counted against the character guideline like any other character. A caret that is not in that caption means the note belongs to the caption as a whole rather than to a place in it, so one is written at each end — `♪ Lyrics ♪`, how a caption that is all music is written — the end first, since writing the opening note would move the end along and the length read before either insert is stale; an empty caption gets a single note rather than a pair with nothing between them. Both paths write through `writeNote()` and report through **one** `onInput()`, so the pair is one undo step. **The icon prevents the default on `mousedown`** — pressing the mouse down anywhere moves the caret, so without it the reader's own caret was already gone by the time the click arrived and every note landed at the end. `splitAt()` reads the caret the same way and carries the same guard. Structural edits (split/merge/add/delete/speaker/timing) are reported to the server rather than left to the browser, which owns the block structure and sends a fresh set back. `delete()` refocuses a neighbouring caption afterward (the one that took its place, or the new last one) the same way `add_after()` already focuses the caption it starts — refresh() removes the deleted caption's own DOM node, and a caret that was inside it does not survive that: left alone, the browser collapses the invalidated selection to the very start of the contenteditable, which reads as the cursor jumping to the top of the page. The same collapse hits a block that was typed into and never got a real render before one finally arrived (undo, most of all — see the revision watcher's own comment): changing that block's key so Vue rebuilds it rather than patches it, needed so undo's own text actually shows, throws away whatever element the caret was anchored to. The revision watcher reads the caret before that happens and, if it was in the block about to be rebuilt, restores it afterward with `placeCaretAt()` — the inverse of `caret()`'s own offset, walking the fresh (server-authoritative, so never carrying `plainText`'s placeholder) text nodes directly, falling back to the block's end if the caption shrank out from under it, and carrying the block's own change in length along with the offset before that — undo (most of all) changes the very text the offset was measured against, and restoring it unchanged into a block that shrank landed past the word the caret was actually at, into whatever now sat where the old, larger offset pointed. `merge()` refocuses the surviving caption at the seam between the two texts for the same reason `delete()` does — merging previous removes the very block the caret was in, and merging next is more useful landing exactly where the two met than wherever the caret already was — `focus()`/`focus_block()`/`focusBlock()` all now take an optional character offset (default 0, unchanged for `add_after()`'s own use) rather than always collapsing to a block's start, `focusBlock()` itself just handing that off to `placeCaretAt()`. `merge_with_previous()`/`merge_with_next()` only actually join the two texts with `"\n"` when both have something to separate — "Add caption after" followed straight away by Backspace merges the empty caption it just started back into its neighbour, and an unconditional join left that neighbour with a trailing blank line it was never typed into. `split()`'s own mid-text branch likewise refocuses the start of the new second block afterward, same as pressing Enter mid-sentence anywhere else — unlike a merge's removal the first block keeps its own DOM node here, so this is about landing where the reader expects rather than avoiding a broken caret; skipped when split_caption refuses the split (nothing on one side of the caret) since no second block exists to focus then. Enter means the same thing in both modes: **Enter starts a new caption** (a new block in a transcription) by splitting at the caret — at the end of a caption there is nothing to move across, so a new empty one is started instead — and **Shift+Enter inserts a literal line break** (`insertLineBreak()`, spliced in by hand — `document.execCommand("insertText", ..., "\n")` looked right but corrupted surrounding content in testing); a break landing at the true end of the caption's text also gets a trailing zero-width space, since a browser reserves no line box, and so no navigable caret, for a forced break with nothing real after it — `plainText()` strips that character back out wherever a block's text is read for anything other than drawing it (`caret()`, the edit reported to the server), so it never reaches the saved caption. Ctrl/Cmd+Enter splits as well, which is what it meant while Enter itself was the line break; nothing intercepts it, so it falls through to the same branch. **The Shift+Enter branch has to stay ahead of the split branch**, which only asks for Enter and would otherwise swallow it. **Paste is reduced to the clipboard's `text/plain` flavour** (`onPaste()`, with `@drop.prevent`/`@dragover.prevent` closing the drag route to the same HTML flavour): left alone, a contenteditable inserts the clipboard's *HTML* — markup chosen by whatever page the reader copied from, and an `<img onerror=…>` in it runs its handler on insertion, in this origin. The text is spliced in as a text node with the same Range technique `insertLineBreak()` uses (never `innerHTML`, never `execCommand`), gets that function's zero-width-space treatment for a trailing `\n`, hands a selection reaching into another block to the server instead (see below), and reports through the same hand-called `onInput()` — so a paste is marked, counted and flushed exactly as typing it would have been.

**A selection can reach across blocks, and no edit over one is left to the browser.** Nothing stops a reader dragging from the middle of one caption into another, and the gutters in between — the numbers, the speaker, the timings — are not editable text: a native Backspace over such a selection deletes those elements along with the text, and the editor comes apart on the page rather than losing the words it was asked to lose. `selectionSpan()` reports the two ends (`getRangeAt` gives them in document order, which is caption order, however the drag went) and `onKeydown` intercepts every key that would edit over one — Backspace, Delete, Enter, and any printable key, which is sent along as the replacement text the way the paste is — plus `onCut()` and `onPaste()`, all emitting **one** `deleterange`. `onCut()` fills the clipboard by hand first, since preventing the default prevents the copy half of a cut too, and builds that text from the blocks' own text (`spanText()`) rather than `selection.toString()`, which sweeps the gutters in with it. `TranscriptEditor.delete_range()` → `SRTEditor.delete_range()` does the whole gesture as one edit and so **one undo step**: the first caption keeps what lies before the selection, the last keeps what lies after, the two become one caption spanning both timings, and the captions between them go. Edit marks are carried by joining every touched caption's marks as if their texts had already been merged (`joined_edits`) and retagging that against what is left (`retag_edits`), so both surviving halves keep their marks and whatever was typed or pasted in earns marks of its own. It returns the seam's offset, which `delete_range()` hands to `focus()` — the blocks the selection ended in are gone from the DOM, and a caret left inside one does not survive the render (the same reason `merge()` and `delete()` refocus). Enter deliberately does **not** also split at the seam: one gesture, one edit, one undo step, and a second Enter there splits as it always does. `block_id()` takes a key name for this event's two ids. The timing rows (`.transcript-time`, `.transcript-subtitle-time`) are `user-select: none` — the gutter already was — so a selection dragged across captions paints only the words it will actually take; the inputs opt back in **on focus only** (`.transcript-time-input:focus`) — a document selection that merely encloses an input still paints that input's own value as selected, so an unconditional `user-select: text` there was itself what kept the timestamps highlighted; focus is enough for editing, since a click focuses the field on mousedown before any drag inside it starts. Covered by `tests/test_delete_range.py` and `TestTimingIsNotText` in `tests/test_caption_editor_styles.py`.

A caption is a paragraph on a page, not a filled cell: giving each one a fill of its own was tried and turned down. `.transcript-text` does set `cursor: text` — the pointer stayed an arrow over the whole editor before, which was the clearest thing saying it was not one. Note that any vertical padding on `.transcript-cell` has to be matched on `.transcript-gutter`: the two are separate items on the same grid row, not parent and child, so padding one alone slides the index and character counts out from under the lines they belong to.

The caption the caret is in marks itself too (`caretId` in the component, `.transcript-cell-editing`), read from the selection in `updateCaretBlock()` on keyup, click, input and `focusBlock()` rather than tracked through edits — the caret moves for reasons nothing in the component hears about. It is deliberately separate from `activeId`, the caption being *played*: the reader is very often editing one caption while listening to another, and `.transcript-cell-editing` is stated after `.transcript-cell-active` so the caret wins the background when both are true. `@blur` now runs `onBlur()`, which flushes exactly as it did and clears the marking with it.

- `srt.py` — `SRTEditor` core: state, caption CRUD, undo/redo wiring (delegates to `undo_redo.py`), `parse_srt`/`parse_txt`.
- `srt_render.py` — validation and the keyboard-shortcuts dialog. No longer draws anything itself — see above. `collect_validation_issues()` is the check itself, separate from the dialog that reports it (`show_validation_report`) and tested on its own in `tests/test_validation.py`: one entry per caption carrying everything found in it, in caption order, split into **errors** (no text, ends before it starts, same timing as an earlier caption, overlaps a neighbour) and **warnings** — the subtitle guidelines, so subtitles only: line length against `CHARACTER_LIMIT`, line count against `MAX_SUBTITLE_LINES`, time on screen against `MIN_CAPTION_SECONDS`. It sets `is_valid` as it goes, so the editor's own red left rule and the report can never disagree. Every caption in the report is a row that jumps to it (`select_caption`), since finding "#47" by scrolling was the one thing the report left the reader to do.
- `srt_review.py` — word-level review/edit marking shared by both formats (`retag_edits`, `review_runs`, confidence flagging).
- `srt_search.py`, `srt_export.py` — search panel and export dialog, shared by both formats.
- `transcript_editor.py` / `transcript_editor.js` — the editor itself, described above.

**Gotcha with `transcript_editor.js`:** NiceGUI registers a custom Vue component (`component="…js"`) once, at class-definition/import time, keyed by file content — a running server keeps serving the old JS until the *process* restarts, not just the browser. `ui.run(reload=True)` is on by default, but uvicorn's reload watcher only globs `*.py` by default, so an edit to this file was silently invisible to a running dev server. `main.py` now passes `uvicorn_reload_includes="*.py, *.js, *.vue"` so edits here trigger the same auto-reload `.py` changes do — if a JS fix still doesn't seem to apply, restart the server by hand and confirm that setting is intact.

## Key conventions

- All authenticated page handlers must call `page_init()` before accessing secret storage keys. `page_init` redirects to `/` when `_scribe_bk` missing in browser storage and refreshes the OIDC token.
- All API calls MUST be async — use `httpx.AsyncClient` (see `db/analytics.py`). Do not introduce blocking `requests` or sync `httpx` calls in new code; legacy sync calls in `utils/helpers.py` should be migrated when touched.
- API calls use `get_auth_header()` from `utils/token.py` for bearer-token auth.
- NiceGUI `ui.table` uses Quasar slot templates with `$parent.$emit('event_name', props.row)` for per-row actions.
- Dialog close on validation failure uses short-circuit pattern: `result and (dialog.close(), navigate)`.
- Use `match/case` statements (Python 3.13+).

## Security

This app holds session tokens, user PII, and an encryption password that gates backend-side data encryption. Treat security as a first-class concern in every change.

### Authentication & session

- Authentication is OIDC. The flow lives in `main.py` (`@ui.page("/")` and `/logout`).
- **The landing page collects the login's tokens with a one-time code, not from the URL.** The backend's OIDC callback redirects to `/?code=…`; `exchange_login_code()` (`utils/token.py`) posts that code to `OIDC_APP_EXCHANGE_ROUTE` and gets `{"token", "refresh_token"}` back server to server, so the tokens never travel through the browser. They used to arrive as `?token=…&refresh_token=…`, which left a working set of credentials in history, in referrers and in every access log on the way. The code is single-use and lives about a minute; the page spends it and clears it out of the address bar with `history.replaceState`. A refused code, and the backend's own `?error=login_failed`, both end as a notification and no session. `OIDC_APP_EXCHANGE_ROUTE` is a new setting — without it nobody can log in.
- Every authenticated page handler MUST call `utils.common.page_init()` first. It:
  - redirects to `/` if `app.storage.browser["_scribe_bk"]` (browser key) is missing,
  - schedules `token_refresh()` on each page load and logs out on refresh failure by clearing `token`, `refresh_token`, and `encryption_password` from `app.storage.user`, then navigating to `OIDC_APP_LOGOUT_ROUTE`.
- Never read `app.storage.user["token"]` / `["encryption_password"]` without going through `page_init()` first.
- Authorization checks: `get_admin_status()`, `get_bofh_status()`, `get_user_status()` in `utils/token.py`. Admin pages must gate on these — do not infer privilege from UI state.

### Secret handling

- Browser-bound encryption: `storage_encrypt` / `storage_decrypt` in `utils/helpers.py` wrap `utils/crypto.py` (`AESGCM` + `HKDF` from `cryptography.hazmat`, base64-encoded ciphertext, random 12-byte nonce, AAD `b"scribe-secret"`).
- Key material = `app.storage.browser["_scribe_bk"]` ++ `settings.STORAGE_SECRET`. Salt = `get_browser_id()`. Never log, print, or echo any of these into UI text, error messages, or exceptions.
- Decryption failure intentionally clears `encryption_password` and redirects to `/` — preserve this fail-closed behavior; do not catch and continue.
- Any new value stored in `app.storage.user` that contains tokens, passwords, keys, or PII MUST go through `storage_encrypt`.
- `STORAGE_SECRET` default in `utils/settings.py` is a placeholder. Production deployments must override via `.env`. Never commit a real secret.

### Input handling

- Filenames coming from users or the API: pass through `sanitize_filename()` (strips `/ \ \x00 < > : " | ? *` and control chars, trims `. ` ).
- Any text rendered into NiceGUI: prefer `ui.label`/`ui.markdown` — avoid `ui.html` with untrusted input. If raw HTML is unavoidable, sanitise with a library like Bleach (`bleach.clean`) rather than ad-hoc regex.
- Validate at boundaries: API responses and form input. Internal helpers can trust their callers.
- Logs must use `user_id`, never usernames, email addresses, or other PII.

### Crypto rules

- Use the existing `utils/crypto.py` helpers. Do not roll new AES/HKDF code; do not switch to ECB, CBC-without-MAC, or PyCryptodome.
- Prefer the secure-by-default `cryptography` package (already a dep). For new crypto needs, evaluate Google Tink before bespoke code.
- Never hard-code keys, IVs, or salts. Nonces must be `os.urandom(12)` per encrypt call (already done by `encrypt_string`).

### Transport & external calls

- All outbound HTTP MUST use `httpx.AsyncClient` (see `db/analytics.py`). Set explicit timeouts. Do not disable TLS verification.
- Backend base URL comes from `settings.API_URL`. Do not concatenate user input into URLs; pass via `params=` so `httpx` quotes them.
- SSRF: if a feature ever fetches a user-supplied URL, block private/loopback ranges before connecting (e.g., an SSRF filter library) — there is no such code path today; flag any addition for review.

### Container & deps

- `Dockerfile` should not run as root in production; pin base image and pip-installed versions.
- Dependencies are tracked in `pyproject.toml` / `uv.lock`. Run `uv lock --upgrade` deliberately, review the diff, and prefer libraries from the "secure-by-default" list (Bleach, defusedxml, Tink) over hand-rolled equivalents.

## Word timings (`utils/srt_review.py`)

`SRTEditor` is `ReviewMixin, SearchMixin, ExportMixin, RenderMixin` composed onto the core defined in `utils/srt.py` (see "Editor split" above). `ReviewMixin.load_words()` takes the optional payload from `GET /api/v1/transcriber/{uuid}/words`:

```json
{"version": 1, "words": [{"t": "Hej", "s": 0.12, "e": 0.34, "c": 0.98}]}
```

- Anything with a different `version`, or an unparseable payload, is discarded — the editor must open normally without word data. Jobs transcribed before word timings existed have none.
- Words are flat and time-ordered, not tied to segments. `caption_words()` maps them onto a caption by time range (bisect on precomputed midpoints), so they survive splits, merges and renumbering. Never key word data by caption index.
- `split_caption(caption, cursor_position=..., text=...)` cuts at the caret and picks the timestamp from the silence between the two adjacent words. Without word data a caret split falls back to proportional; **a split with no caret must keep halving the caption**, which is what results predating this feature rely on — and that halving goes through `split_point()`, which takes the gap between words nearest the middle, looking **both** ways. Searching backwards only and settling for the bare middle when nothing was found cut straight through the first word of any caption with no space before its midpoint. A caption of one long word has no gap and keeps the middle; every real caption has one.

A position the reader actually chose is honoured to the character, mid-word or not — only made-up ones get snapped, which is why `splitAt()` sends **no offset at all** when the caret is not in the caption it was clicked on, rather than the middle of the text dressed up as a caret. `TranscriptEditor.split()` treats a missing offset as "halve this"; note it checks `offset is None` rather than falsiness, since `0` is a real caret position (at the very start, where the split is refused for having nothing on one side).
- `c` is absent when the worker ran with `WORD_CONFIDENCE=false`. Gate the review controls on `editor.has_confidence`, not on the presence of `words`.
- The editor renders review/edit marking as structured runs (`review_runs()`, one dict per run: `t`/`flag`/`edit`/`s`/`e`), consumed directly by `transcript_editor.js`'s Vue template rather than as an HTML string — never build caption markup inline. `get_review_html()`/`review_backdrop_html()` produce the same marking as escaped HTML for callers outside the live editor (e.g. tests asserting on the marking itself); nothing in the UI renders them today.
- Live edit marking (the `[data-changed]` attribute `onInput` sets, styled green only under `.transcript-show-edits` — see `TranscriptBody.set_show_edits`, and `build()` syncing it from `editor.show_my_edits` before the first render, since that toggle is a saved preference and can already be on when the page loads) is a client-side stand-in for `review_runs()`'s own `edit` flag, needed because `set_text()` deliberately never re-renders the block being typed into. `spaceAtTail()` stops a space typed at a word's own tail from marking that word — the browser extends the same span for it, "nowhere else for it to go" — but the next *real* character removes that trailing whitespace, so `effectiveSpan()` remembers the crossing as `newWordBoundary` (a span+offset pair, forgotten if the caret ever dips back below it — editing the original word again) and, the moment real content exists past it, calls `splitNewWord()` to move that content into a new sibling span of its own before marking it — marking the original (shared) span would mark the old word too. A second space typed right after the first extends the boundary instead of splitting: there is still nothing but whitespace past it, so nothing to give a span of its own. The one place this reaches into the DOM directly rather than only reading it; safe because `dirty` already guarantees whatever shape it leaves gets discarded and rebuilt at the next real render regardless. `markChanged()` itself refuses to mark a span of pure whitespace no matter which path hands it one — `wordAt`/`effectiveSpan` both work at DOM element boundaries, not word boundaries, so a caret can legitimately land inside a separator's own span, and there is no word there to highlight. The mirror case — typing a new word in *before* an existing one, caret at the boundary between a separator and the word right after it — resolves into the separator's own tail too, the same way `spaceAtTail` exploits for the word-before-a-space case, but nothing here remembers a boundary across keystrokes the way `newWordBoundary` does, since the whitespace prefix never changes: `effectiveSpan()`'s `headMatch` branch (`/^(\s+)(\S+)$/` against the span, caret at its tail) fires and calls `splitNewWord()` on the very first real character, isolating the new word immediately rather than waiting for a second keystroke to cross anything. Splitting alone still leaves the new word flush against the one after it with nothing between them in the DOM — fine while the reader keeps typing, since nothing has been sent yet, but the debounced `flush()` 400ms out does not know that, and a natural pause mid-word sends the two run together (`"thereworld"`) as an undo-able state, with `retag_edits()` then correctly, if unhelpfully, marking that merged token as edited once undo lands on it. A synthetic space (a bare `Text` node, `this.syntheticSpace`) is appended right after the new word's span the same moment it is split off, so every flush from there on stays properly spaced even before the reader finishes typing a separator of their own; `spaceAtTail()` firing against that same span — the reader's own trailing space — retires it, so a real one and the synthetic one already standing in for it never both persist. `retireSyntheticSpace()`, called at the top of `onInput` before the caret or the text are read, covers the other way that space stops being needed: the reader backspacing the new word away again. `spaceAtTail`'s own retirement cannot catch that — it only fires when a real space is typed — and left in the DOM the space is read straight back out as part of the caption, flushing a stray double space the reader never typed as its own undo-able state (and, in subtitleMode, counting against the character guideline). "No longer needed" is everything ahead of it inside its own parent being whitespace: the new word had a span of its own, and the browser takes that span away with the last character of it.

### Words flagged for review

One control, **Off / Low / Medium / High**, highlights words the model was least sure of and picks how far up the confidence range to flag, via `REVIEW_SENSITIVITY_*` in settings. Raising the level must only ever flag more.

It sits on the **same row as the switches** ("Follow audio", "My edits", and the subtitle-only "Subtitle overlay"/"Timeline"), as a group of its own — label, selector, count, on a tighter `gap-2` inside the row's own `gap-4` — so a narrow pane wraps the three as one piece rather than splitting the label off its selector. It had a second row behind a `ui.separator()`, on the reasoning that a selector is a different kind of control from a switch; what that actually did was put two of the things that mark the text ("My edits" and the flagged words) in two different places, and cost a row of a pane the Analyse strip's answer area is trying to grow into. `.editor-settings` stays a column all the same: that row is what `show_player()` folds away with the video, and a narrow pane wraps it into more than one.

There was a separate "Uncertain words" switch alongside a Low/Medium/High selector; "off" is simply the lowest setting of the same thing, and splitting them meant two places to look to find out whether anything was being flagged at all. The model underneath is unchanged — `show_uncertain_words` (bool) plus `review_sensitivity` — and the control writes both: "off" sets the flag false and **leaves the stored level alone**, so coming back lands where the reader left it, and `REVIEW_SENSITIVITY_KEY` is never written the string `"off"` (it is not one of `REVIEW_SENSITIVITIES`, so `set_review_sensitivity` would reject it anyway). The selector is the only review-coloured control now, so `.q-toggle.review-switch` is gone and only `.bg-review-accent` carries `--color-review-accent`.

- **Every flagged word is marked identically** — one class, one shared tooltip. The score is not a calibrated probability, so it cannot support grading flagged words against each other, and the raw number is never shown anywhere.
- **The marking must not read as an error.** No red, and no wavy underline (that means spellcheck). It uses `--color-review-*`, a violet reserved for this and used nowhere else.
- The flagged counter comes from `flagged_word_count()` over the whole word list, and reports 0 while the switch is off. Anything that changes the switch or the sensitivity must call `update_flagged_count()`.

## Undo history (`utils/undo_redo.py`)

`save_state()` snapshots an `EditorState`: the captions **and** the speaker set, copied so a later edit to the live editor cannot reach back into a state already on a stack. Both halves have to travel together — the captions carry which speaker each block belongs to, the list carries which names exist at all, and the list cannot simply be recomputed from the captions on the way back, since a speaker can legitimately exist with no block using it ("Add new", or every block reassigned away). Restoring only the captions left a block naming a speaker the list no longer held: the menu highlighted nothing as current, `prompt_rename` refused to act on that name (it checks membership), and the name introduced by the undone edit lingered as unused forever.

`EditorState.speakers` is `None` for a state saved without one, and `SRTEditor.restore_speakers()` treats that as "this state has nothing to say about the speakers" — it leaves the live list alone rather than emptying it. The set is mutated in place rather than rebound, because the speaker menu and `TranscriptEditor.refresh()` both read it straight off the editor.

`undo()`/`redo()` return the `EditorState`, not a bare caption list, and both call `update_flagged_count()` — the text moving is exactly what changes which words are flagged, and that counter is pushed rather than recomputed on read.

Because the speaker list rides the snapshot, anything that changes it needs a history entry of its own: `add_speaker()` and `remove_speaker()` both call `save_state_for_undo()`, without which they were not undoable *and* the next unrelated undo silently took them back.

Find and Replace (`replace_in_current_caption`, `replace_all` in `utils/srt_search.py`) retags edit marks inline via `retag_edits()` rather than going through `update_caption_text()`, which would take a second snapshot on top of the one already saved — Replace All across many captions has to stay one undo step. Assigning `caption.text` directly, as both once did, skipped retagging altogether: the replaced word came out unmarked, and a replacement of a different length left every mark after it pointing at whatever word had shifted into its position.

## Editor keyboard shortcuts

Two handlers, split by what each needs to know:

- **Block-scoped** (in `transcript_editor.js`, gated on `event.ctrlKey || event.metaKey`): Ctrl/⌘+Enter split (Enter alone does this too), Ctrl/⌘+↑/↓ move a word across the boundary, Ctrl+M merge with next, Ctrl+D delete, and — subtitles only — Ctrl/⌘+Shift+Enter add after, the keyboard's half of the caption row's own "+", which a transcription has no row for. **One list per format**: `onKeydown` only picks, on `this.subtitleMode`, between `subtitleShortcut()` and `transcriptionShortcut()`, each of which answers `true` when it took the key and `false` to let it fall through to the split branch and the page's own handler. They are deliberately not one shared list — the two agree today, add-after aside, since a paragraph is split, merged, deleted and trimmed exactly as a caption is, but giving one format a key must not silently give it to the other. The server handlers (`TranscriptEditor.merge`/`move_word`/`delete`) never cared which format they were in. Each key has to know which block the caret is in — which the page's document-level handler does not. Both lists report through one `report()` helper, which `preventDefault()`s and `flush()`es before emitting, so a pending edit reaches the server before the structure changes under it. **The Shift+Enter branch must stay ahead of the split branch**, which only checks for Enter plus a modifier and would otherwise swallow it.
- **Document-scoped** (in `SRTEditor.handle_key_event`, driven by the page's `ui.keyboard`): save, export, find, undo/redo, play/pause, and Ctrl+Shift+V validate (subtitles only — a transcription has no line-length or line-count guideline to check).

`moveword` is the event that finally reaches `move_first_word_to_previous()`/`move_last_word_to_next()` in `utils/srt.py`; both had existed and been tested for a while with **nothing calling them**. `TranscriptEditor.move_word()` refocuses the block being trimmed rather than following the word across, so a second press means the same as the first. Both captions are re-timed from the word data by the editor methods themselves.

The Shortcuts dialog is worded for whichever format is open — captions or paragraphs, never "block", which says nothing to either reader — and its contents come from `SRTEditor.keyboard_shortcut_groups()` (title from `keyboard_shortcuts_title()`), separate from `show_keyboard_shortcuts()` that draws them so the wording is testable without a UI, the same way `collect_validation_issues()` is separate from its report. Add-after and validate are the two rows subtitles alone carry.

**Modifier choices are deliberate, not oversights.** Ctrl only (never ⌘) for M, D and Shift+V: ⌘+M minimises, ⌘+D bookmarks, and ⌘+Shift+V is paste-without-formatting on a Mac, and taking any of them would take them from the whole window. Note also that macOS binds Ctrl+↑/↓ to Mission Control at the OS level, so the ⌘ variant is the one that actually reaches the page there — which is why the word moves accept both.

## Following the video (`TranscriptEditor.follow_video`, `pages/srt.py`)

`video.on("timeupdate", transcript.follow_video)` (`pages/srt.py`) drives two separate things from one server round trip, a linear scan of `editor.captions` for the one whose `[get_start_seconds(), get_end_seconds())` window contains the player's current time (`ui.run_javascript` reads `document.querySelector("video").currentTime` — timeupdate itself carries no position):

- The editor's own active-block highlight (`TranscriptBody.set_active` → `activeId` → `.transcript-cell-active`, and the scroll that follows it) — gated on `editor.autoscroll`, the "Follow audio"/"Autoscroll" switch.
- The subtitle overlay drawn over the video itself (`TranscriptEditor.set_overlay`/`draw_overlay`, subtitles only — `pages/srt.py` only creates and wires the label when `data_format == "srt"`, since a transcription's own blocks are a speaker's whole turn, not a short timed cue, and would cover half the frame) — **not** gated on `autoscroll`: it is a preview of what a viewer sees, wanted whether or not the reader also has the editor scrolling to follow along. Hidden (`set_visibility(False)`) between two captions or past the last one, the same as a real subtitle track would be. Gated on its own switch ("Subtitle overlay", `editor.show_subtitle_overlay`, persisted under `OVERLAY_SHOW_KEY`) — **on** by default, unlike the review markings, since it shows what a viewer would see rather than adding anything to read; `set_overlay_enabled()` redraws at once rather than waiting for the next timeupdate, which never comes while the video is paused, and that is exactly when the switch tends to be used. `draw_overlay()` splits the caption on its own `"\n"` and gives each line its own `ui.label` (`.video-subtitle-line`), so the overlay breaks exactly where the editor does rather than wherever the width happens to run out — the same split `caption_line_counts()` uses for the character guideline, and a blank line still gets an element of its own so the lines under it do not move up. Labels, never `ui.html`: a caption's text is user content, not markup to trust. `refresh_overlay()` redraws it after an edit as well as on timeupdate — the reader is very often paused while editing, and timeupdate does not fire then, so the overlay would otherwise keep showing the caption as it read before. Hooked into both `changed()` (typing, which `set_text` deliberately reports without a re-render) and `refresh()` (split, merge, delete, retime, undo — any of which can change *which* caption covers the moment being played, not just what it says). It re-derives the caption from `overlay_seconds`, the time the player last reported, rather than holding a reference to the caption itself: undo and the structural edits swap the `SRTCaption` objects out wholesale, and a kept reference would point at one no longer in the list. Nothing redraws before the first timeupdate has given it a time to work from. `_set_overlay_text()` only redraws when the caption actually changes, since timeupdate fires several times a second and mostly lands on the same one — the remembered value starts as the `UNDRAWN` sentinel rather than `None`, which is itself a real value here ("nothing is playing") and would otherwise swallow the first tick that lands in a gap. Each line is drawn `white-space: nowrap` and the overlay's own `font-size` is `min(1rem, calc(85cqi / (var(--subtitle-char-limit) * 0.55)))` — a wrap inside a line the editor did not break shows a viewer a break the subtitle file does not have, so instead of letting the box wrap, the type shrinks with the frame until a line of the guideline's full length fits on one. `--subtitle-char-limit` is set on the overlay element in `pages/srt.py` from `settings.CHARACTER_LIMIT` rather than hard-coded in the stylesheet, and `.video-frame` carries `container-type: inline-size` for `cqi` to resolve against the video's own width (the splitter can leave it narrow). `.video-frame` gives the video's wrapping `div` a `position: relative` neither `ui.video` nor the card around it otherwise has, for the overlay's `position: absolute` to anchor against; `pointer-events: none` on the overlay keeps it from swallowing clicks meant for the native controls or a click-to-pause anywhere else on the frame. `bottom` is a fixed `3rem`, not a percentage of the frame's own height -- a percentage once put the overlay on top of the native control bar (a fixed height itself) on a short or wide video, hiding the seek bar underneath its own opaque background rather than sitting above it.

**A click on a caption always goes to that caption.** `time_at_offset()` answers `None` for a word that cannot be placed in the recording (one the reader wrote, or one the model transcribed without a timing), and `seek()` used to do nothing at all with that — no seek, no overlay, no active block, which reads as the editor having missed the click. The refusal is right only while the recording is **inside that caption already**: that is the case the rule was written for, a reader listening to a caption and clicking a word in it, where jumping back to its first word is worse than staying put. Coming from anywhere else the click is asking for the caption itself, there is no position within it to refine, and `seek()` falls back to its start. "Anywhere else" is decided from `overlay_seconds`, the last position the player reported; before it has reported at all, nothing says the recording has left the caption, so the rule stands as written.

**A seek the editor asked for itself does not wait to be told about it.** `follow_video` now only reads the player's time and hands it to `TranscriptEditor.moved_to()`, which is what marks the active block and draws the overlay — and everything that moves the recording deliberately (`seek()` from a click in the text, `select_from_timeline()`, `create_caption()`) calls `moved_to()` with the time it just asked for. Setting `currentTime` to the value the player already holds moves nothing and so fires **no timeupdate at all**, and the first caption of a recording usually starts at 0, exactly where a freshly opened player sits: clicking it seeked nowhere, nothing reported back, and it alone drew no overlay and lit no block.

There is no bisect index over caption time windows the way `caption_words()` has one over word midpoints (`utils/srt_review.py`) — `follow_video` re-scans the full caption list on every tick, which is fine at typical caption-list sizes and timeupdate frequency.

## Caption timeline (`utils/speech_timeline.*`)

Follows [SUNET/scribe-ui#126](https://github.com/SUNET/scribe-ui/issues/126) — read it before changing the component's shape; the decisions below are its decisions, not incidental.

A strip under the video showing where someone is talking, where the silences are, and where the captions divide the recording. **Not a waveform**: the runs come from `ReviewMixin.speech_runs()` over the word timings the transcription already carries, so nothing is decoded and the recording is not fetched a second time — decoding an hour of audio in the browser costs over a gigabyte of PCM, and `utils/video.py` answers every request with the whole file (it returns `206` without actually slicing a range), so a second fetch is a second full download. What it cannot show is loudness, music or noise; a real waveform needs peaks precomputed server-side (ffmpeg) and served as their own endpoint.

- `speech_runs(gap=0.3)` merges words closer together than `gap` into one run — the space between two words in a sentence is not a silence anyone means, and one bar per word draws a picket fence. Words with no timing are skipped rather than placed anywhere.
- `speech_duration()` is the later of the last word's end and the last caption's end: a caption can be stretched past the last thing said and still has to fit on the strip. The component takes the later of that and the video's own `duration` once the player knows it, since a recording usually runs on past the last word.
- **The playhead is not a prop.** The component finds the `<video>` itself and follows it — `timeupdate` several times a second, plus `requestAnimationFrame` while playing (timeupdate alone is visibly steppy across a strip this wide) — and clicking seeks by setting `currentTime` directly. A round trip per tick to move a line two pixels is not worth taking.
- **The playhead is fixed in the centre** and the recording scrolls behind it. Deliberately *not* clamped at either end: clamping slides the playhead off centre exactly where the reader works most, on the first and last captions. What lies before 0 or past the end is shaded (`--timeline-void`) rather than left as blank strip.
- **Three states, never conflated**: the caption being *edited* (`currentId`, sent from the server — that is where it is known), the caption being *played* (`playingId`, computed locally from the playhead, since pushing it would be a round trip several times a second), and the caption under the *pointer* (`hovered`, local). Hovering one on the strip must not take the text editor's focus off another, which is why `onLeave` clears only the hover.
- **A caption is a bracket pair**, `[` and `]`, not a block: when one caption ends exactly where the next begins — the common case — a block cannot say which of the two touching edges belongs to which caption. The whole interval is the hover target, so nobody has to aim at a thin line; the brackets thicken when hovered, which is also when they can be grabbed. Each carries its own `#number`, drawn only when it fits, so the same caption can be found in the text editor.
- **Speech is secondary to caption timing** (the issue is explicit): drawn quietly along the top in `--color-text-muted`, with the captions below it in the brand colour.
- **A caption made this way has no timed words in it**, whatever it covers: its text was typed by hand, so it aligns with none of the words under it (`aligned_words()` returns all `None`), and a caption dragged out of a silence has none under it to begin with. `time_at_offset()` — which `seek()`, a click in the text, goes through — falls back to the caption's own start when *nothing* in it is timed, exactly as it does for a job with no word data at all. Its "an edited word has no honest time to move to, so stay put" rule is about telling one edit apart from the timed words around it and needs there to be timed ones; without the fallback, clicking such a caption in the text moved the recording nowhere, so the subtitle overlay went on showing whatever the player was still parked in — or nothing, when that was a stretch no caption covers. Note the check is over the *aligned* words, not `caption_words()`: a caption dragged over speech that had no caption — the whole point of the feature — does cover words, and testing for those found the silent case only.
- **Clicking a caption asks for that caption** — `selectcaption` → `select_from_timeline()` seeks **to its start**, focuses the block in the text editor and marks it current. Clicking the strip itself asks for that moment, to the pixel, and changes nothing else — that click never reaches `select_from_timeline()`. It once seeked to the middle of the caption, on the reasoning that the frame where a cue begins shows what is about to be said rather than what the cue covers; that was reported as a bug, since a reader clicking a caption is asking to play it and landing halfway through means the first half is never heard without seeking back by hand. A caption *just dragged out* still gets its middle (see below) — there the start is the edge the reader has this second placed themselves. `mark_current()` is also called from `seek()` (a click in the text) and `scroll_to()` (search, autoscroll), so all three ways of moving to a caption agree.
- **It can be dragged to the foot of the page.** A grip (`⠿`) in the legend row moves it — not the strip itself, which already means taking hold of a caption, and one gesture cannot mean two things. Letting go below `DOCK_AT` (70% down the window) docks it; above, it goes back under the video, and the legend says which while the grip is held. Docked it is `position: fixed` from where the menu rail ends to the right edge — the rail is fixed too, so `left: 0` runs underneath it, and it is not a fixed width either: `followTheRail()` measures `.q-drawer--left` and the component watches it with a `ResizeObserver`, a `transitionend` (Quasar animates the rail) and a window `resize` — rendered through `<Teleport to="body" :disabled="!docked">` while docked — `position: fixed` alone was not enough, because the strip lives inside the splitter and the splitter's separator is a positioned element that drew a line straight down through it. Teleport moves only where it renders: same component instance, same listeners, same state — and it holds `DOCKED_WINDOW = 60` seconds instead of 20: three times as wide, so roughly the same seconds per pixel, which is what actually decides whether a caption edge can be seen and aimed at. A fixed strip takes no space of its own, so `markDocked()` puts a `timeline-docked` class on `document.body` and the stylesheet pads `.nicegui-content` out of its way. The position is persisted under `TIMELINE_DOCK_KEY`, but the prop is called `startDocked` and is only read at mount: the component owns the position from then on, and a prop pushed back mid-session would yank the strip out from under the reader.
- **It shows twenty seconds at a time undocked** (`WINDOW = 20`, within the 10–30s the issue asks for), because an hour across the pane is roughly a minute per pixel and no caption edge can be seen at that scale, let alone aimed at. Fixed, not offered as a choice — a strip whose scale changes underfoot is harder to read, not easier — and a recording shorter than the window simply shows all of itself. The view follows the playhead, centred on it and clamped at both ends (a window scrolled past the start would show empty space where the recording is not). A label says which part is on screen (`12:30–13:30 of 42:15`), since one stretch of speech looks much like another.
- **Dragging where no caption is makes one.** The strip is where a reader can see that nothing covers a passage of speech, so it is where they can say that something now does — rather than adding a caption after some other one and dragging it across. `startCreate()`/`onCreateMove()`/`finishCreate()` mirror the retime drag: both ends go through the same `snap()` (so a new cue meets its neighbour flush, or lands on the ends of a phrase), and both are kept inside `gapAt()` — the stretch around the point pressed that the captions leave free — since two cues over the same instant is one of the errors "Validate" reports and a gesture should not be able to make one by accident. `create_caption()` re-checks the overlap on the server (captions can have moved since the strip was last drawn) and otherwise appends, `sort_captions()`s (a caption's number *is* its position, and this one was appended rather than inserted), and then does everything a click on the strip does — **seeks the recording to the middle of the new caption** (not its start: that is the edge the reader has this second placed themselves, so there is nothing to show them there — unlike a *click* on an existing caption, which does go to its start), hands it the caret the way "Add caption after" does (it starts empty, and its text is the next thing wanted), and marks it current. The seek is what keeps the frame under the subtitle overlay honest: the overlay draws whatever caption covers the play position, and a caption dragged out of the empty stretch around the playhead — fixed at the centre of the strip, so that is usually where the empty stretch is — covers it, so the text appeared over a frame from somewhere else entirely as it was typed. A gesture that barely travelled is still a click, and means that moment: `finishCreate()` refuses anything under `MIN_NEW_SECONDS` and leaves `onClick` to seek. While in flight it is drawn like any other caption under `NEW_CAPTION` (`0`, which `renumber_captions()` can never produce) and without a number, since it does not have one yet.
- **A caption can be dragged**: an edge moves that edge, the middle moves the whole cue, and an edge is taken before the body when the pointer is within `EDGE_GRIP` of one. `captionAt()` picks the **nearest** edge of any caption, not the first caption in the list that happens to be in range: two cues close together both answer to a point between them, and taking whichever came first meant reaching for one caption's end and getting the next one's start — and a caption shorter than the grip could only ever be taken by its start, since that check came first while both edges were in range. A tie is broken by **which side of the edge the pointer is on**: where one cue ends at exactly the instant the next begins the two edges are the same instant, so approaching from the left takes the first cue's end and from the right the second cue's start. Any fixed preference makes one of the two unreachable — a caption butted against its neighbour could not have its start dragged at all. The hover readout names the edge (`caption #12 end`) so this is visible before the drag starts. A dragged edge snaps to **the edges of the other captions first** (`CAPTION_SNAP`, a wider reach than anything else): two cues meeting exactly is something a reader means, while a cue landing a hundredth of a second from its neighbour — a gap no viewer can perceive, but a gap in the file — never is. Only when no caption is in reach does it fall back to the edges of a run of speech (`SNAP`), which is where a cut belongs when it is not against another cue; failing both, it lands where the pointer is. The dragged caption is excluded from its own candidates, or it would snap to the edge it is being pulled away from. The drag continues off the strip (listeners on `window`) and the caption is drawn where it *would* land while it is in flight. On drop it emits one `retimespan` carrying both ends, which `TranscriptEditor.retime_span()` applies through `apply_time()` — which calls `SRTEditor.sort_captions()`, since a caption's number *is* its position in the list and a retime can move it past a neighbour: without it the text editor went on showing the caption where it used to be, numbered to match neither its own timing nor its bracket on the strip (the sort is stable, so two captions starting at the same instant keep the order they had). One event, so **one undo step**: two retimes would mean pressing undo twice to put back one gesture. A refused drag (an end before its start) calls `refresh()`, since the strip is already drawing the caption where it was dropped and nothing else would put it back. A click event follows the mouseup that ended a drag, so `onClick` ignores one after any real movement — seeking to wherever a drag finished is never what was meant.
- **The "Timeline" switch is a prop (`shown`), not `set_visibility()`.** NiceGUI hides an element by putting a `hidden` class on it, and this component's template is rooted in a `<Teleport>` — not a DOM node, so a fallthrough class has nothing to land on and the switch did nothing at all. `SpeechTimeline.set_shown()` sets the prop; the wrap is `v-show` (not `v-if`, so the canvas and the listeners `mounted()` set up survive), `markDocked()` ands it into the body class so a hidden docked strip stops reserving its padding, and the watcher calls `settle()` on the way back, since a strip that was `display: none` has no size to have measured itself against.
- Only the runs, the captions and the duration are sent. `TranscriptEditor.refresh_timeline()` redraws the captions from `refresh()`, alongside the overlay, since a split, merge, retime or undo moves them; the runs never change.
- **`draw()` runs on every animation frame while the recording plays**, so what it does per frame matters: the canvas is resized only when its size actually changed (assigning `canvas.width` reallocates the backing store) — which is why `shrinkCanvas()` clears the remembered size along with the pixels: hiding the strip and showing it again hands back exactly the box it had, the size string matched, and `draw()` painted into the 0×0 store `shrinkCanvas()` had just left, the custom properties are read once into `palette` and dropped only when they could have changed (a `MutationObserver` on `document.body`'s class for the theme, and docking, which moves the strip somewhere with a different background), and only the runs and captions overlapping the visible window are drawn — `within()` bisects into each time-ordered list and steps back over a long entry that begins before the window and reaches into it. `visibleCaptions()` adds the dragged caption back in, since it is drawn where it *would* land, which can be outside the window its own timings still put it in.
- A canvas cannot read a stylesheet, so the colours are custom properties on `.speech-timeline` that the component asks for by name — which is what lets dark mode change them without the component knowing. It redraws on `ResizeObserver` (the splitter changes its width) and at `devicePixelRatio` (or the playhead lands between pixels).
- **It names what it draws.** Bars, gaps, ticks and a playhead mean nothing on their own, and a canvas has no elements to hang a tooltip on — so a small key sits under the strip (each swatch drawn as the shape it stands for, not a uniform square), and pointing anywhere on the strip shows a readout of the time, whether anyone is speaking there, and which caption covers it, worked out from the same runs and boundaries it draws. The readout is `pointer-events: none`: it sits over the strip the reader is trying to click.
- Offered only for subtitles, and only when `editor.words` is non-empty — there is nothing to draw without timings, and nothing to time in a transcription, whose blocks are a speaker's whole turn rather than a short cue (the same gate the subtitle overlay has). The same component-registration gotcha as `transcript_editor.js` applies: keyed by file content at import, so a `.js` edit needs the process restarted.

## On a phone

What a phone is for here is **recording something and starting a transcription of it**. Editing is not: the editor puts a video, a caption list and a timeline side by side, which is a desk. `/srt` says so for itself on a small screen (`.editor-too-small`, `pages/srt.py`) rather than being shrunk into something that technically fits — a sentence and a way back, not a redirect, because a reader who followed a link there should be told what to do rather than bounced to where they came from. Which of the two is drawn is left to the stylesheet: the server cannot know how wide a phone is, and a reader turning a tablet in their hands should not have to reload.

**One breakpoint, 700px** — the difference between a window and a hand, not a scale of sizes. Below it:

- **The menu rail gives its width back.** 56px of a 390px screen went to icons nobody was looking at. Collapsed it is 0 wide; opened it is drawn *over* the page rather than beside it, since there is no room to put anything beside anything. Quasar writes the page container's padding inline from the drawer's own width, which is why the rule has to say `!important`.
- **A finished job is *viewed*, not edited.** The card's action for a completed job says **View** and opens `/view` (`pages/view.py`), a read-only page: the recording at the top (sticky, since the point of tapping a passage is hearing it — sticking *below* the header, whose height is measured in the page into `--view-header`: the header is `position: fixed`, so a sticky element stopping at 0 parks underneath it and loses its top edge, and the height is not a constant because the header carries the announcement banners and gets shorter when one is dismissed), the transcription under it, the passage being played marked, and a "Follow audio" switch that scrolls the list with it. It is the same fetch and the same parsing the editor does — an `SRTEditor` is built only to run `parse_srt`/`parse_txt` over the result, so what a caption is (its timing, its speaker, where the paragraphs fall) is decided in one place for both pages — and nothing on it writes: no save, no export, no `TranscriptEditor`, no editor state on the server behind it. Marking and seeking are done **in the page** rather than driven from the server the way the editor's own `follow_video` is: a round trip several times a second to move a highlight is not worth taking when nothing on this page holds state. A caption's text is drawn with `ui.label`, never `ui.html` — it is somebody's speech. Which page a row opens is the *caller's* decision (`table_view` vs `table_click`, both onto `open_result()` in `utils/common.py`), not something worked out again from the width of the screen: the card emits `table_handle_row_view`, the desktop row still emits `table_handle_row_click` and still says Edit. `/srt`'s own too-small notice offers the same page ("View transcription") rather than only turning a phone away.
- **A row of the jobs table becomes a card.** Seven columns are unreadable on a phone and the row's own action — Transcribe, or Edit — is the whole of what anyone came to the list for. Quasar's `grid` prop does the drawing and the table's `item` slot fills the card in: filename, type and status, the dates, a checkbox so Delete still works, and the action as a full-width button. **The width it switches at and the width the stylesheet dresses the cards at are the same number**, and `tests/test_mobile_layout.py` is the only thing that says so — cards styled for a hand appearing at a different width than the cards themselves is a stretch of widths nobody laid out.
- **`window.innerWidth < 700`, not `$q.screen.lt.md`.** NiceGUI evaluates a `:`-prefixed prop with `eval()` in the *page's* scope, where Quasar's `$q` — a component property — does not exist; a prop that throws is dropped without a word, which is what happened. Read once, when the table renders, which is what a phone needs; a desktop window dragged across 700px keeps its columns until the page is opened again.
- **Nothing may be wider than the screen.** Several cards name a minimum width — 400px for the upload dialog, 500px for the shortcuts dialog and the user settings card — and a minimum wider than the phone takes the whole page sideways with it. The upload dialog asks for `min(400px, 100%)`; the rest are answered by the stylesheet (`min-width: 0 !important` on cards in a dialog and cards in the page), since there are too many to keep finding by hand.
- The table's toolbar stacks and its four actions — Delete, Export, Transcribe, Upload — become a two by two grid of identical cells (`.jobs-actions`, `display: grid`), since they are peers and none of them is the action of the page; wrapping them sized each by its own label, which put three on one row and one alone on the next. Four across a 390px screen leaves no room to read "Transcribe", which is why it is a grid and not `flex: 1 1 0`. The product name in the header truncates; the recorder's waveform takes the full width and its buttons a thumb's height.

Checked in a browser at phone width, not merely written: the cards, the overlay drawer, the record dialog, the transcription settings dialog and the editor's notice, with the desktop layout confirmed unchanged at 1400px.

## Information dialog (`pages/srt.py`)

What is open and what is in it — file name, language, caption count, reading speed — behind an **Info** button in the toolbar rather than laid across it: it is read when a reader wonders, not while they work, and a dialog has room to say what each figure means instead of leaving a row of bare numbers to a tooltip.

The two moving figures (`captions`, `wpm`) are labels registered by name through `SRTEditor.set_status_elements(**labels)`, and `update_status()` redraws them — it fills only what the page registered, so `status_values()` still answers `duration` (and `format_duration()` still gives `m:ss`/`h:mm:ss`) for any caller that wants it. The dialog does not: a running time taken from the last caption's end says where the subtitles stop, not how long the recording is, which is what a row called "Length" is read as. The count is worded for the format — captions or paragraphs, in both the row label and the figure itself, never "block" — and that word is the *only* thing the two formats' rows differ in: one set of rows, naming the recording "Media file" and explaining each figure in terms of the transcription it came from, with `counted` ("captions"/"paragraphs") standing in for the block noun. Each row is a small bold heading with its value under it and an italic sentence saying what the value means (`.editor-info-row` is a column, not the two-column form it was: a fixed 8rem label column left the filename, the one value with no room to spare, whatever was left). **`renumber_captions()` is what calls `update_status()`** — it runs after parsing and after every structural edit, which is the only hook that always fires; registering the labels happens while the toolbar is built, long before the captions are parsed, so without it the dialog reported an empty editor for the whole session. The file name and the language never change, so they are drawn once.

## Keeping a session (and unsaved edits) alive

The captions being edited live in that client's own `SRTEditor` **on the server**. Anything that reloads the page or ends the session throws them away back to the last save, so three separate reload paths are deliberately handled:

- **`@ui.page("/srt", reconnect_timeout=300)`** (`pages/srt.py`). The default is 15s (`main.py`), which also sets the socket ping (`ping_interval` 12s, `ping_timeout` 6s) — so ~18s of stalled connection ends it. Past the timeout NiceGUI **deletes the client's content** (`nicegui/client.py`) and the browser reloads itself on return (`nicegui.js`, "reloading because handshake failed"). A closed laptop lid, a wifi hop or an idle proxy was enough. Other pages keep the short default: nothing unsaved lives on them.
- **Refreshing distinguishes "refused" from "could not ask"** (`utils/token.py`). `token_refresh_call()` returns `None` only for a 4xx — that session is genuinely over — and raises `RefreshUnavailable` for a network error, a 5xx or an answer with no token. `token_refresh_or_wait()` turns that into "keep the session" until `MAX_REFRESH_FAILURES` in a row (10 × the 30s timer in `page_init` ≈ five minutes of an unreachable provider). Before this, `except (httpx.HTTPError, Exception): return None` meant one blip logged the reader out mid-edit.
- **The OS-theme reload is only for chart pages.** `reload_on_theme_change()` exists because Plotly is drawn server-side in one theme's colours and cannot restyle itself; it is called by `analytics.py`, `groups.py` and `health.py` rather than by `page_init`, which used to install it on every page — including the editor, where a Mac switching to dark at sunset reloaded the captions away. Everything else follows the theme through CSS custom properties. (The manual dark-mode button further down `page_init` still reloads, and already skips `/srt` for the same reason.)

A fourth thing the timer has to survive is its own session ending. It keeps firing after a tab is closed, a server restarts or a reader logs out, and `app.storage.user` **raises** for a session NiceGUI no longer holds rather than answering — so `refresh()` asks `session_alive()` first. Without it the first sign of a session ending is an `AssertionError` traceback out of `nicegui/timer.py`.

`tests/test_session.py` covers all four. What none of them covers is a browser crash or a closed tab — that needs a local draft, which does not exist yet.

## Analysing a transcription (`utils/inference.py`, `utils/inference_panel.py`)

The **Analyse** strip under the video: pick summary, study notes, key points or questions, and watch the answer arrive a few words at a time. The request goes to the inference hub (served on its own port by the API in transcribe-backend) and on to a GPU worker; see that repo's own notes for the other end.

**Transcriptions only.** `pages/srt.py` builds the strip on `settings.INFERENCE_ENABLED and data_format != "srt"`, and `InferencePanel.build()` returns without drawing anything for a `srt` editor as well — the same rule stated where the strip is. A caption is a line cut to fit a screen rather than a sentence, and a subtitle file is the same speech the reader already has in front of them; it is a lecture, a meeting or an interview that is long enough to be worth asking about. `transcript_text()`'s own subtitle branch (joining the lines back into prose) is kept and still tested — it is what makes the function honest about both formats — but nothing in the UI reaches it now.

**A line of the answer goes back to where it was said.** The finished answer is not one markdown element: `text_blocks()` cuts each prose part into the passages a reader clicks — paragraph, heading, list item, with a fenced code block kept whole since its blank lines are part of it — and each becomes its own `.inference-passage` with a click handler. A bullet in a set of study notes is about one moment in the recording; the notes as a whole could only ever mean one of them. Clicking one calls `locate_caption()` (`utils/inference.py`), which is the whole trick: the answer **quotes nothing** — it is a model's paraphrase — so there is no marker to follow back, and the passage is traced by the words it and the transcription share, each weighted by an inverse document frequency over the captions so a name or a technical term decides the match and "and" does not. Words the transcription never uses are left **out of the reckoning** rather than counted as misses, since half of any sentence in an answer is the model's own wording. A caption has to account for `JUMP_MIN_SCORE` of the shared weight across at least `JUMP_MIN_TERMS` separate words, or nothing is placed — one shared word is a coincidence however rare it is, and moving the reader on the strength of it is worse than saying the line could not be placed (`NOT_FOUND`). Ties go to the earlier caption, which is where it was said first.

Four things decide whether that actually finds the passage, and each was a way it missed:

- **A line is traced to a *run* of captions, not to one.** A bullet summarises a stretch of speech, and a transcription's caption is a whole speaker turn, so the words behind one line are routinely spread over two or three of them and no single caption clears the threshold — the usual miss was no caption at all for a line that plainly came from somewhere. Runs of up to `JUMP_WINDOW` are scored, each caption past the first costing `WINDOW_DISCOUNT` so a run only wins when it genuinely covers more than the best single caption in it does.
- **The reader is put down at the run's start**, not at its strongest caption (`_landing()`), the same reason a click on the timeline seeks to a caption's start: landing halfway through means the first half is never heard without seeking back by hand. "The start" is the first caption carrying at least `LEAD_SHARE` of what the run's best caption carries, so a run opening on a caption that shares one common word does not send the reader a caption early.
- **Inflection is folded away** (`fold()`, applied by `terms()`). The recordings are mostly Swedish, where the definite article is a suffix — "budget" is "budgeten" the second time it is mentioned — and the model uses whichever form its own sentence wants, so an exact match on the surface form misses most of what the two texts share. `ENDINGS` is stripped repeatedly (one ending sits on another: budgeten → budget → budg) down to `MIN_STEM` and capped at `MAX_STEM`. Blunt on purpose: the same folding is applied to both sides, so over-folding merges words rather than losing them.
- **A word most of the transcription uses cannot be what a jump rests on.** The idf is the smoothed `log((1 + total) / (1 + appears))` plus `TERM_FLOOR` (without a floor, a word in every caption weighs nothing and a short transcription becomes unplaceable), and separately anything appearing in more than `COMMON_SHARE` of the captions is barred from counting toward `JUMP_MIN_TERMS`. Otherwise a sentence of the model's own about the answer itself — "This answer was generated from the transcription." — shares only "the" and "was", which are all of it the transcription uses at all, accounts for its whole weight, and is placed confidently somewhere arbitrary. The jump itself is `TranscriptEditor.go_to()`, handed to the panel as `on_jump` by the page: seek, `moved_to()` (so the overlay and the active block follow without waiting for a `timeupdate` that a paused player never sends), and `scroll_to()` — deliberately **not** `focus()`, unlike `select_from_timeline()`: a click on the strip is a click on the captions themselves, while a click in the answer is a click on something else about them, and it must not take the caret out of whatever the reader was typing into. Without an `on_jump` the passages are still drawn, just not clickable, and `JUMP_HINT` is left off the line under the answer. The split is only done once the answer is finished: half a paragraph is not a passage worth following back to the recording, and rebuilding the element tree on every batch would flicker for the whole length of an answer — and a batch that arrives after that rebuild (the hub is still sending when a reader presses Stop) puts the streaming element back rather than being dropped into passages that are already drawn.

**Landing somewhere is not the same as being shown it.** Both ends of the jump are marked. In the text, `TranscriptEditor.go_to()` calls `TranscriptBody.flash_block()`, which sends a `flash` prop — `{"id": …, "n": counter}`, an **object with a count**, because an id alone does not change when the same caption is asked for twice and a prop that does not change is a watcher that does not fire, so the second jump would land unmarked. The component's `flash` watcher clears `flashedId` and sets it on the next tick (the class has to actually leave the element for the animation to restart), then drops it again after 2.4s; `beforeUnmount` clears the timer. `.transcript-cell-found` is a **ring that fades**, not a tint: the caption's background already carries three states between them — playing, being typed into, a search match — and a fourth colour there would be one more thing to tell apart, while fading is itself what says this answers "where did I land" rather than naming a state the caption is in. Under `prefers-reduced-motion` the ring is simply held instead. In the answer, the passage that was followed keeps `.inference-passage.is-followed` until another is followed — it is the only thing on the page saying which of a dozen similar bullets the transcription was moved for — and a passage that could **not** be placed is left unmarked, since marking it would claim the transcription is showing where it came from.

**Under the video, not behind a toolbar button or in a dialog.** It is read alongside the transcription — a reader checks a summary against what was actually said — and a dialog covers the very text being checked.

**Nothing at rest, though.** Two icons riding the right end of the switch row (`.inference-launcher-row`, held there by `margin-left: auto` rather than the row's own `justify-content`, since they are one item of it and not its layout): ✦ opens the assistant, ✎ opens the review. `pages/srt.py` makes the slot inside that row and `build_launchers()` fills it much later, once the panels exist — an element's place in the pane is where it was *created*, so the slot has to be made where it belongs. A subtitle editor has no assistants and gets no slot rather than an empty one. They were in the video frame's own top corner first, which needed a translucent ground to stay readable over a moving picture; on the row they need none and cost no row of the pane at all. Press one and its panel opens in the space under the video and fills the rest of the pane; close it and the space goes back to the recording. It was a permanent row — a mark, a pill per task, Review, and a status line under them — on screen whether or not anybody was using it, which cost two rows of a pane the answer itself is trying to grow into and made the pills the busiest thing in the editor. The tasks did not go away: `self.actions` is now the row at the top of the assistant's own panel, read when it is wanted. `.inference-panel` is `flex: 0 0 auto` and takes no room until `_set_open()` puts `is-open` on it (`flex: 1 1 0%`) — a flex item left to grow holds the space it is not using away from the video. The launchers are hidden until the hub answers, and a hub with no worker connected leaves them disabled **with `NO_WORKER` as their tooltip**: nothing else is on screen at rest to carry the reason, and a disabled icon without one reads as a fault in the page.

**Both panels are drawn in the same space, so one is open at a time.** `open_panel` (`None` / `"analyse"` / `"review"`) is the whole of that state and `_set_open()` the only thing that changes it. `open_analyse()` refuses while a review is up — ending one properly is the assistant's own business, with a request in flight and a queue of decisions behind it. `close_analyse()` keeps whatever answer is in the panel, since generating it cost a GPU somebody paid for and closing a panel is not asking for it to be thrown away; it also folds the video back if `toggle_expand()` had folded it away, or the pane would be left with neither. `start(task)` calls `_set_open("analyse")` itself, so a task pressed from anywhere is visible. The answer box is hidden until there is an answer, for the same reason the timeline has a fixed height: text arrives a few words at a time, and a box growing on every batch would push the video up the page while the reader watches it.

**Both panes carry the same definite height, and it is measured rather than guessed.** One rule — `.editor-panes .editor-panel { height: var(--editor-height, calc(90vh - 100px)) }` — covers the card in each half of the splitter, and a small script in `pages/srt.py` sets `--editor-height` on the splitter to `window.innerHeight - <the panes' own document-relative top> - 12`, remeasured on `resize` and by a `ResizeObserver` on `.nicegui-content` (the toolbar above them wraps on a narrow window, which moves where they begin without the window changing size). The `calc(90vh - 100px)` fallback is the expression this replaces, kept so a pane is never height-less before the first measurement: it matches no actual piece of chrome — a tenth of the window plus a hundred pixels — and left a band of empty page under both panes, more of it the taller the window. It has to be the card and not something inside it: a NiceGUI card has a `1rem` padding of its own, so while the transcript side once put the height on its scroll area, that card came out 2rem taller than the video card and the two ended at visibly different points down the page — the review's footer stopping short of the transcript's last line is what showed it. The transcript's scroll area takes what the padding leaves (`flex: 1 1 0%; height: auto; min-height: 0` — `height: auto` to shed NiceGUI's own `16rem` on a scroll area). `h-full` alone measured nothing: a Quasar splitter panel is sized by its content, so a percentage height inside it resolves to auto and the pane simply ended wherever the switches stopped. With a definite height the strip (`flex: 1 1 auto; min-height: 0`) takes what the video, the timeline and the switches leave, and the answer box grows into it. `.video-frame` keeps `flex: 0 0 auto` and, importantly, **no `min-height: 0`** — with one, flex happily shrank the video to nothing at all.

**Exported as its own file, in its own words.** `export_document()` puts a title and one line — "Generated from the transcription by Sunet Scribe. Not part of the transcript." — at the top of every download, and `_download_name()` names it after the media file and the task (`lecture-action-items.txt`). Both exist because the assistant's output and the transcription's own export land in the same downloads folder, and [SUNET/scribe-ui#75](https://github.com/SUNET/scribe-ui/issues/75) requires the derived output to be unmistakable for the transcript. Plain text is the default the issue asks for; `plain_text()` strips the Markdown rather than shipping a `.txt` full of `##` and `**`, which is a worse read than the page it came from. Markdown is the second item in the same menu, **Word the third** (`export_word()`, `utils/docx_export.py`) — a set of study notes is handed in, pasted into a course page or marked up by somebody who does not have Scribe open, and `.docx` is what those readers have — and **LaTeX the fourth** (`export_latex()`, `utils/latex_export.py`), for the reader at the other end: notes going into a document somebody is already writing in LaTeX, or formulae to be typeset rather than read on a screen.

**The Word export keeps a formula a formula** (`utils/omml.py`). An answer's mathematics is LaTeX, drawn in the page as MathML by markdown2's `latex` extra; Word understands neither, and a formula exported as text is one nobody can edit, search or trust. `latex_to_omml()` goes LaTeX → MathML (`latex2mathml`, already a dependency) → OMML, Word's own math markup. The usual route is Microsoft's `MML2OMML.XSL`, which ships with Office and is not ours to redistribute, so the mapping is done in Python instead — a mapping and not a parser, since latex2mathml has already done the hard part and what arrives is a small, predictable set of elements. What is not covered falls back to its own children, so an unknown wrapper costs its decoration rather than the whole formula, and a formula that will not convert at all comes back as the LaTeX it was rather than failing the export. Two things are decided across siblings rather than one element at a time: a `\left(…\right)` pair arrives as three siblings and has to become one delimiter object or the brackets do not grow with what is between them, and an n-ary operator (a sum, an integral) folds everything after it inside as its own operand. A displayed formula (`$$…$$` alone on a line) becomes an `m:oMathPara` **inside a `<w:p>`, not beside one** — Word refuses the whole document otherwise, saying only that there is a problem with the contents, with every other part of the file perfectly good; an answer whose formulae are all inline opens fine, which is what made it look like a problem with one particular answer. **No `<m:ctrlPr>` is written anywhere**, empty or otherwise: it carries the formatting of an object's own control characters, an empty one says nothing, and what Word accepts is settled by what Word accepts — pandoc, whose `.docx` output it has taken for years, writes none. pandoc is the reference for this whole part of the file: it also reads `.docx` back, so `pandoc -f docx` is what verifies an export here, and its own `-t docx` output is what a doubtful construct gets compared against.

**The LaTeX export is the Word one's mirror image.** What the `.docx` has to convert is the one thing `.tex` needs no conversion for — the model writes its formulae in LaTeX, so `$…$` and `$$…$$` are passed through untouched. Everything *around* them is the work instead: LaTeX reserves ten characters that a transcription of somebody's speech uses freely (per cent signs, ampersands, an underscore in a file name), and one unescaped one is a document that does not build. `escape_text()` does them in **one pass** rather than one replacement after another, since three of the replacements contain braces of their own and a second pass would escape those — `\textbackslash\{\}` where a backslash was meant, which is exactly the bug the test pins. Prose is escaped and formulae are not, and knowing which is which is the whole reason both exports go through `utils/markdown_blocks.py` rather than over the raw text. Sections are starred (`\section*`), so LaTeX does not number them over the top of whatever numbering the answer already uses; a Markdown list is a flat run of items each carrying a depth and `_list_environment()` reads it back into the nested environments LaTeX wants; a code fence is `verbatim`, so a program's own backslashes and braces stay as written. The preamble is deliberately the packages in every installation there is (`inputenc`, `fontenc`, `geometry`, `amsmath`, `amssymb`, `hyperref`): an answer that will not build because the reader lacks a package is no better than one that will not build because of an ampersand. It is a whole document rather than a fragment to `\input` — a reader who wants a fragment can cut the middle out, while a reader given a fragment has to write a preamble before they can see anything.

**Emphasis is read by recursing into it, not by taking it as text.** `**Integrera $z$:**` is a bold heading with a formula in it, and flattening what an emphasis encloses printed the dollars in the Word document while the page rendered the equation. `inline_runs()` therefore calls itself on the contents and carries the formatting down as arguments, so a formula nested in a bold bullet is still an equation and a subscript inside an italic phrase is still italic. `inline_latex()` already did this; only the Word writer flattened.

**A model closes and reopens a display fence mid-derivation** — `$$ … $$$$ … $$`, meaning the next line of the same working. Read as one formula reaching from the first fence to the last, the `$$$$` in the middle is four characters of *mathematics*, and that is exactly how it reached the reader: `$$$$` set in the equation, in the middle of an integral. `display_formulas()` reads a paragraph as the formulae it is made of — an empty list when it holds anything else — and both exports draw one equation per formula, which also puts each line of the working on a line of its own, as a derivation is written. A fence with nothing at all between its dollars is dropped rather than printed (the `empty` token), since it is not a formula and never was.

**An ordered list carries on across whatever interrupts it.** A model writes a derivation as numbered steps with a displayed formula under each one, and a blank line before a formula ended the list — so the three steps came out numbered 1, 1 and 1. An item that numbers *itself* 2 means the list it just wrote, whatever stands between them, so `parse_markdown()` reuses the previous ordered list's id for any item whose own number is greater than 1 and starts a fresh one at 1. Word gets one numbering definition and counts straight through; LaTeX needs telling, since a second `enumerate` starts at 1 however the blocks were numbered, and gets a `\setcounter{enumi}` — not enumitem's `[resume]`, which would be a package the reader has to have.

`utils/markdown_blocks.py` is the reading of the answer that both share: `Block`, `parse_markdown()` and the `INLINE` tokenizer. Not a Markdown implementation — it reads what the models actually write. Doing it once is what keeps the two exports from disagreeing about what the answer said.

**A model writes inline HTML among its Markdown, and every export has to read it.** `R<sub>H</sub>` and `10<sup>-18</sup>` are how a model writes a chemical formula or a power of ten when it is not writing LaTeX; the page draws them, since `ui.markdown`'s sanitiser allows those tags, so nothing was wrong until the same answer was exported and the reader got the tags themselves in their document. `INLINE` therefore knows `<sub>`/`<sup>` (Word: a `w:vertAlign` run, **last in `w:rPr`**, which is where the schema puts it; LaTeX: `\textsubscript`/`\textsuperscript`; plain text: `R_H` and `10^-18`, which is how anyone would have typed it on one line), reads `<b>`/`<strong>`/`<i>`/`<em>` as the emphasis they are alongside Markdown's own marks, keeps `<br>` as a break, and **drops** the rest rather than printing it. HTML entities are resolved back to their characters (`plain()`) before each format does its own escaping — otherwise `&amp;` reaches a `.tex` file as an escaped ampersand-a-m-p. The tags that are dropped are **named one by one**: a catch-all for anything between angle brackets would eat "a < b and c > d" out of somebody's speech, which is a thing a lecturer says and a model transcribes faithfully. `link_of()` exists for the same class of reason — the link's label and address are read back with a regex of their own rather than by group number, since every addition to `INLINE` shifts the numbering and does it silently.

`utils/docx_export.py` is the document around that: a `.docx` is a zip of XML and it is written by hand. python-docx would be the obvious dependency, but it would have to be broken open for the maths anyway (it has no idea what an equation is), it drags lxml — a C extension — into the container for it, and the part of the format needed here is small: a body, a stylesheet, and a numbering definition for the lists. **Element order is the schema's, not ours** — `<w:rPr>`'s children go `rFonts`, `b`, `i`, `color`, `sz`, and `<w:pPr>`'s go `pStyle`, `keepNext`, `numPr`, `shd`, `spacing`, `ind` — and Word calls a file that gets it wrong corrupt rather than saying what is wrong with it. Every list gets a numbering definition of its own, so an ordered list following another starts at one again. A style the document names but does not carry is no style at all in Word, which is why `styles_xml()` defines the few that are used. `tests/test_docx_export.py` checks the shape of the package rather than what it looks like: that every part named is in it and parses, and above all that a formula comes out as an `m:oMath` and not as its own LaTeX — the one outcome the feature exists to prevent, and the one that looks fine until somebody opens the file. Verified end to end by reading an exported file back with `pandoc -f docx`, which recovers every equation as LaTeX.

**What the answer cost is shown, not only recorded.** The hub's `done` message carries `input_tokens`, `output_tokens` and `gpu_seconds` — the last added for this, alongside the two already there — and they are the *same* figures the hub writes to its `inference_usage` row, so what a reader is shown and what gets billed cannot drift apart. `usage_of()` reads them off the message (zeros for a worker that reports none, so callers have one shape), `add_usage()` sums them and `usage_line()` writes the line: thin-space-grouped thousands, and the GPU clause dropped when a worker reported no timing rather than printing `0.0 s`. Nothing reported at all gives an empty string — a row of zeros says less than no row. The Analyse strip shows the last answer's cost at the right of the status line (`.inference-usage`, quieter than the note beside it: there to be found, not read every time) and adds "… this session" once more than one question has been asked of the recording. The review assistant shows its own total — both requests, the classification and the review — in the summary at the end, never on a card being decided on. `InferenceClient`'s `on_done` therefore takes one argument now; every handler was updated with it.

**One button per task, no picker.** The tasks come from the hub (`fetch_tasks()`), one pill each at the top of the assistant's panel, so adding a task in `utils/prompts.py` puts a button here without touching this file. They are peers, and none of them is the action of the page — Save is. The pressed one carries a Quasar `loading` spinner while it runs and the others are disabled rather than hidden, so the row does not reflow under the pointer.

**Every button in the strip says `color=None`**, and `test_no_button_in_the_strip_asks_for_a_colour` keeps it that way — the same rule and the same test the review assistant has. NiceGUI colours a button `primary` unless told otherwise, which puts Quasar's own `.text-primary` on it, and that carries `!important`: the task pills came out in the brand blue whatever `.inference-chip` said. Asking for no colour leaves them inheriting the page's own text colour.

**The pills are quiet, not filled.** They were filled in the near-black the transcription is set in, on the reasoning that outlined pills read as disabled beside the switches above them. With a fifth added for Review that came to five solid blobs in a pane 40% of the window wide, wrapping onto two rows and reading as the loudest thing in the editor. So: no fill and no border at rest, `--color-text-secondary`, and `--color-bg-surface-hover` under the pointer — the same treatment `.inference-icon-btn` already has, so the row reads as one set of controls rather than two, and the hover carries the weight the fill used to. Review is no longer among them at all: it has a launcher of its own, since it goes through the transcription and offers to change it rather than producing text to read. `load()` still does not return early on an empty task list — a hub offering nothing else can have domains to review against, and that launcher alone is reason enough to show them.

**The model is not a reader's choice.** The hub picks a worker that has one and `model=None` goes on the wire. Readers cannot tell the aliases apart, and a choice made blindly would still be recorded against their usage as though they had meant it. Comparing models is an operator's job, done by changing `INFERENCE_MODELS` in the worker. `_accepted()` deliberately does not print the alias either.

**The answer is always in the language that was spoken**, and there is no control offering otherwise — a Swedish lecture summarised in English is no use to the person who recorded it. The job's own language is sent (`answer_language()`) rather than left to the model to infer: the prompts are written in English, and a small model asked for "the same language as the transcript" will cheerfully answer in the language it was addressed in. The job's language carries qualifiers the model has no use for — "Swedish (verbatim)" is a transcription mode, "Northern Sámi (Experimental)" is a note about our support for it — so the parenthetical is cut before it is sent, and a test checks every `WHISPER_LANGUAGES` entry survives the hub's own validation pattern once cut.

**The socket belongs to this process, not to the browser.** NiceGUI already holds a socket to the page, and the editor already lives on the server, so the answer is streamed into a `ui.markdown` from Python — no Javascript of our own, and the reader's token never goes near a page script. One `InferenceClient` per open editor page, opened on the first request and closed by `on_disconnect`, at which point the hub cancels whatever that connection asked for: a reader who shuts the tab must not leave a GPU generating for nobody.

**Nothing is saved, and the dialog says so.** What gets sent is `transcript_text(editor)` — the captions as they stand right now, including edits not yet saved, which is what a reader means by "summarise this" — and the answer lives in the page until it is closed. That is the whole point of the design (no second copy of somebody's recording anywhere), so the download and copy buttons are the persistence, and the sentence explaining it sits under the result rather than in a tooltip.

`transcript_text()` differs by format for a reason: subtitle lines are cut to fit a screen, not into sentences, so they are joined back into prose before being sent; a transcription keeps its speakers, since who said what is most of the meaning in a meeting — except `UNKNOWN`, the editor's placeholder for an unassigned block, which would otherwise have the model attribute half the meeting to a person called Unknown. Timings are never sent: they cost tokens and say nothing about what was said.

**Maths renders, and the way it does is what decides how the answer is sanitised.** `ui.markdown(sanitize=True)` — NiceGUI's default — runs the browser's `setHTML`, or DOMPurify where that is missing, over the rendered HTML before inserting it. That is a real sanitiser, and it is what makes this safe; an earlier version escaped `&<>` by hand instead, which stopped markup and formulae alike. Verified in a browser: an answer carrying `<script>` and `<img onerror>` produces neither element and sets nothing on `window`. **Maths** is `markdown2`'s `latex` extra (hence the `latex2mathml` dependency), which turns `$…$` and `$$…$$` into MathML that the browser draws itself. No Javascript, no web font, nothing to load. The prompt asks for it (`FORMATTING_GUIDE` in the backend's `utils/prompts.py`), because a model will not reach for it on its own: a lecture's formulae come back spelled out in words. Confirmed with gemma3 on a physics transcript — `$KE = \frac{1}{2}mv^2$`.

**No diagrams.** A ```mermaid fence in an answer was once drawn as a diagram by the copy of mermaid NiceGUI ships, and a good deal followed from it: prose and fences had to be split apart (`split_answer()`, because the sanitiser strips the class markdown's own mermaid support keys on, so a diagram written inside markdown rendered as its own source), a fence had to be checked against the diagram types mermaid knows before being handed over (`is_diagram()`, or a model writing sentences into a fence got an error box), `click` directives binding a node to a script had to be cut out of it, and the Word export had to have the browser rasterise the picture — mermaid is Javascript, so nothing on this side can draw one — with mermaid reconfigured for `htmlLabels: false` so the labels survived rasterising at all. **All of it is gone.** A fence is a code block like any other, in the page and in every export. The models were not good enough at diagrams for any of that to be worth carrying, and an answer about somebody's lecture is not where that gets discovered. The generating end went with it: `FORMATTING_GUIDE` in transcribe-backend no longer asks for a fence, so nothing at either end produces or draws one.

The task list, the model list and the input limit all come from the hub at dialog-open time (`fetch_tasks()`) rather than being configured here twice: offering a reader a model no worker is running only produces a refusal one click later. The model picker is drawn only when there is more than one — one model is the normal case, several means somebody is comparing them.

## Review assistant (`utils/review_assistant.py`)

Follows [SUNET/scribe-ui#74](https://github.com/SUNET/scribe-ui/issues/74) — read it before changing the flow. **Review**, the last pill in the Analyse strip, goes through the transcription looking for likely mishearings and offers them one at a time: Accept, Dismiss, Skip. It is a review tool and **not an editor** — nothing reaches the captions that the reader has not pressed Accept on, and nothing is saved by it.

**Two hub tasks, in order.** `review_domain` classifies the recording; the reader confirms or changes that; `review_suggestions` then reviews it against the confirmed domain. Both live in transcribe-backend's `utils/prompts.py` with `offered=False`, so they never appear as pills in the Analyse strip — the assistant asks for them in its own order. The domain matters: a model reviewing a law seminar as though it were cardiology suggests confident nonsense, and the reader knows the material better than the classifier does. The domains themselves are **OECD FORD levels 1 and 2** (what SCB/UKÄ's Swedish research subject classification is built on), served by the hub in the same `/inference/tasks` payload the strip already fetches — plus two entries FORD has no place for, `0.1 Academic administration` and `0.2 General`, since a departmental meeting is neither a research field nor honestly filed under one. The list is the hub's to extend: `read_domain_code()` matches whatever codes the payload names rather than a copy kept here, so adding a domain server-side needs no frontend release. A code is matched longest-first (`2.11` is not `2.1`), and a model that answered with a *label* has not answered the question — the picker opens with nothing chosen rather than a confident wrong classification.

**One socket, one menu, one slot.** The assistant shares the Analyse strip's `InferenceClient` (one socket per open editor) and its catalogue: `InferencePanel` takes an `on_catalogue` callback and hands the hub's answer straight on, which is what the page turns into `inference.set_review_available(...)` — the ✎ launcher is drawn hidden and shown only when the hub named domains *and* has a worker connected, since an icon that apologises one click later is worse than no icon. It also borrows the strip's *slot*: `container` and `on_close` are assigned in `pages/srt.py` right after `inference.build()`, alongside `client`, because none of the three exists before then, and `available` counts a missing container as "not offerable" the same way it counts a missing client. Transcriptions only, the same rule the strip has.

**JSON Lines, not prose.** `review_suggestions` answers with one JSON object per line (`find`/`replace`/`kind`/`why`), which is why that task carries neither the formatting guide nor a Markdown answer: `parse_suggestions()` reads what it can and drops what it cannot, so one malformed line costs one suggestion rather than the whole review. A suggestion whose `find` equals its `replace` is dropped as no suggestion at all, and `MAX_SUGGESTIONS` caps a model that will not stop. `why` is the one thing written in the language of the recording (`answer_language()`), and it is drawn with `ui.label` — generated text about somebody's speech is never markup here. Every label, button and heading around it stays English, as the issue asks.

**Accepting.** `apply_suggestion()` changes **every** place the text appears, as one undo step: a name spelled two ways through a recording is the case the feature exists for, and accepting caption by caption would mean pressing Accept a dozen times for one decision. It takes the snapshot itself and retags edit marks inline for the same reason Replace All does (`update_caption_text` would take one snapshot per caption). The replacement is applied through a **function** rather than a replacement string — a `\g` or a backslash in a model's suggestion is text, not an instruction to `re`. Matching is by word boundary where the text begins or ends with a word character, so replacing "dos" does not reach into "dosering"; a phrase ending in punctuation still matches, which is why the boundaries are conditional rather than a plain `\b…\b`.

**The reader decides while the model is still writing.** `_collect()` parses the answer on every batch and appends whatever is newly complete (`_take_new_suggestions`), so the first suggestion replaces the spinner as soon as one line has arrived rather than at the end of a long generation — a partial last line is not an object yet, so `parse_suggestions()` ignores it and nothing half-arrived is offered. The queue is **appended to, never rebuilt** from the answer: Skip moves a suggestion to the back of that same list, and rebuilding it would undo the reader's own decisions. A later arrival never redraws the card (that would move what is being read, several times a second) — only `progress_label` changes, and it says "3 so far, still looking…" until `_done` clears `streaming`.

**Each suggestion is shown in its sentence.** `_draw_context()` puts the caption the match sits in on the card, the matched words picked out, cut back to `CONTEXT_CHARS` either side with `excerpt()` and line breaks flattened — "dos" against "dose" cannot be judged on its own, and sending the reader to look it up in the transcription for every suggestion is asking them not to bother. Three `ui.label`s laid out inline (`.review-context > * { display: inline }`), never one piece of HTML: a caption is somebody's speech and a suggestion is a model's writing, and neither is markup to trust here.

**The replacement is the reader's to change.** The "Suggested" value is an input, not a label: a model that heard the wrong word has usually heard the right *kind* of thing, and a reader who can see what was meant should not have to dismiss the suggestion and go and find the caption to type one word into it — editing it on the card keeps everything else the card is worth, every occurrence changed at once as one undo step with the sentence it came out of in front of them. `Suggestion.suggested` keeps the model's own wording (filled in by `__post_init__`), so `edited` can tell a reworded accept from an untouched one and the summary can count them; `applicable` is what Accept is enabled on — an emptied box, or the transcribed text typed back in, asks for *no* change, which is Dismiss, and `_accept()` re-checks it rather than trusting the disabled button, so an emptied suggestion can never reach the captions as a deletion nobody asked for. The typed value is stripped on accept, since it goes straight into a caption. Only the replacement is editable: `find` is the anchor into the transcription — what the occurrence count, the excerpt and the staleness check are all worked out from — and a reader who wants to change *that* is looking for Find and Replace, not a review.

**It is drawn in the strip, not over the text.** A suggestion is judged against the transcription it came out of, and the excerpt on the card is a sentence while what settles a mishearing is often the paragraph around it. As a dialog this meant a card standing on the very text being consulted, and everything that followed from that: a `seamless` dialog so the page behind stayed live and scrollable, a `drag_indicator` header as the handle (never the card itself, which holds the replacement box and the excerpt where a drag is a text selection the reader meant), a `DRAG_SCRIPT` run in the page since a pointer moves several times a second, and both ends clamped to the window so a card dragged off the bottom could still be dragged back. **All of that is gone.** `_build()` draws the header, `.review-body` and `.review-footer` into `self.container` — the strip's own `review_slot`, which sits where an answer sits — so the review is beside the transcription and cannot be in the way of it. The two share the room and only one is up at a time: `InferencePanel.start_review()` calls `_set_open("review")`, and `end_review()` (the assistant's `on_close`, reached from the header's ✕ and from every Cancel/Stop/Close in the footer) closes the space again. Nothing has to be muted while a review is up, because the assistant's own panel — pills, Copy, Download and all — is not on screen; opening it over a live review is refused instead. **It fills the pane downwards.** `.review-panel` and `.review-body` are both `flex: 1 1 0%` — basis 0, not auto, so the height comes from the room the video, the timeline and the switches leave rather than from the suggestion on show: a short card still reaches the foot of the pane, a long one scrolls between the header and the footer instead of pushing past them, and `.review-panel`'s `min-height: 0` is what lets the body scroll rather than grow the panel out of the pane. That also replaces the fixed `19rem` the dialog's card had, and serves its purpose better: Accept/Dismiss/Skip are pressed dozens of times in a row and must not move, and a pane-driven height cannot be moved by a long explanation. Nothing about the position is remembered, and there is nothing to remember. **Everything that draws checks `_open()` first**: `close()` empties the slot and drops `body`/`footer`, while a cancel only races whatever the hub already put on the wire, so a late batch would otherwise draw into a slot that is gone.

**Undo last accept.** In the footer, enabled only when there is one. Accepting is a single undo step, so `editor.undo()` is what puts the text back and the assistant remembers nothing but which suggestion to offer again (and how many captions it had changed, for the summary). Deliberately only the *most recent* accept: reaching further into the editor's history than the change the assistant itself made would be undoing the reader's typing.

**Skip is not Dismiss.** Dismissing is a decision and is final; skipping is the absence of one, so the suggestion goes to the back of the queue and comes round again — and skipping everything ends the review rather than looping. Both say so in a tooltip, since two grey words side by side do not. **The three answers are drawn once for the whole review** (`_review_footer()`, guarded by `deciding`) and only the card above them is redrawn: they are pressed dozens of times in a row, and a button rebuilt — or moved, because the card above it grew — is a button the reader has to find again every time. `.review-body` scrolls inside its own box for the same reason, its height coming from the pane (`flex: 1 1 auto` under `.review-panel`'s `min-height: 0`) rather than from what is in it. They are also not blue: Quasar paints a flat button in the primary colour, three blue words in a row read as three links, and Accept is the only one of the three that touches the transcription — so `.review-action` sets the page's own text colour and `.review-primary` alone carries the brand. A suggestion whose text is no longer in the transcription is dropped as stale (`_current()`) rather than offered against text nobody can see: the queue was written against the transcription as it was when the review started, and accepting one changes it. The summary at the end counts all four outcomes and says outright that nothing has been saved.

## Recording into the upload dialog (`utils/recorder.py`)

**Upload** on the jobs table is a menu, not a button that opens one dialog: *Upload files* or *Record audio*, and `table_upload(table, mode=…)` draws one or the other. They are separate dialogs rather than one holding both — the reader has already said which they came for, and a recorder sitting under a drop target is a second thing to read past every time a file is uploaded. Underneath they are the same hidden `ui.upload`, so a recording and a dropped file take exactly the same path from there: the temporary table row, the byte progress, `post_file`, all of it. `_dropzone()` and `record_panel()` are the two halves.

**The whole gesture is wired in the page**, not through Python handlers: `getUserMedia` wants the click that asked for it, and a round trip to the server and back is not that click any more. One `SCRIPT` string with `__NAME__` placeholders the panel fills in with element ids — deliberately not `str.format` or `string.Template`, both of which would collide with `{}` and `$refs` in the Javascript.

**A microphone is only offered in a secure context**, which in production means the browser being on https — TLS ends at the proxy and this app is plain http behind it, and `window.isSecureContext` is about the browser's URL, so recording works there. The gate checks `isSecureContext` **before** it checks for `MediaRecorder`, since over https the answer is never "use https" and a message that says so sends whoever reads it looking in the wrong place. A `getUserMedia` refusal is named rather than reported as a failure: over https it is nearly always the browser's own permission or a `Permissions-Policy` header from the proxy, and those are fixed in different places by different people.

**Which microphone is the browser's business until it is worth asking.** Two things decide that, and neither is ours: a browser reports **no device labels** until the microphone has been granted once for the origin — a list reading "Microphone 1 / Microphone 2" is not a choice anyone can make — and most machines have exactly one input. So `listDevices()` draws the picker only when the names are known *and* there is more than one, and otherwise the browser's own default is used silently, as before. It fills itself in on the first grant and again on `devicechange` (a headset plugged in mid-session); the listener sits on `navigator.mediaDevices`, which outlives the dialog, so `_recordCleanup` takes it off again. A chosen device is asked for with `{deviceId: {exact: …}}` rather than as a hint: a browser substitutes another microphone for a deviceId it cannot honour, which is the one outcome a picker must not have — so it fails, the default is used, and the reader is told that is what happened. The options are written with `textContent`; a device name is whatever the operating system says it is. It is a native `<select>`, not a `ui.select`, because a QSelect's options live on the server and these change in the page. The choice is not persisted: the dialog is built fresh each time it is opened.

**The waveform is the card** (chosen from five mockups). It is tall enough to actually read, and the clock and the REC marker sit *inside* it rather than in a caption underneath, so there is one thing to look at instead of four; each carries a ground of its own, without which the digits fall in among the bars and cannot be read. The card shows **exactly one filled action at a time** — Start recording, then Stop, then Use recording — with Play the only outlined button and only ever beside Use, and Record again and the device line as the quiet row under them. `setState()` is the single place that says what each state shows, so a state that forgets an element is a change to one function. **There is no `<audio controls>`**: the browser's own player was the loudest thing in the card and the least like the rest of the app, so play is a button of ours and the waveform is the scrubber. The buttons are built with the icon and the label as *children* rather than a `q-btn`'s `icon` prop — Play becomes Pause in the page, which a prop would need a round trip for, and a prop-set icon carries its own margin that a child does not, so mixing the two spaced one button's icon differently from the rest. They keep the app's usual `color=black flat`; without it Quasar paints every label in its own primary blue. While it runs the bars carry the same red as the REC marker: this is the one place in the app where red means "in progress" rather than "wrong", which is why the word REC is there to say it in words as well.

**The waveform is drawn from the live stream, never by decoding the recording.** An `AnalyserNode` on the stream that is already open costs nothing, works at any length, and is the only thing on screen saying the microphone is picking anything up while it runs; `decodeAudioData` over an hour of audio is more than a gigabyte of PCM — the same reason the caption timeline draws speech runs instead of a waveform. One peak per `PEAK_MS` bucket is collected and kept after the recording stops, so the preview redraws from the same array and decodes nothing either. A column takes the **loudest** peak it covers, or a quiet sample would be drawn over a loud moment beside it. The analyser is deliberately not connected to the destination: that is a microphone played back through the speakers it can hear. Colours are read from the page's own custom properties per draw, since a canvas cannot read a stylesheet.

**Nothing is uploaded until the reader has heard it back.** Stopping draws the take and offers *Play*, *Use recording* and *Record again*; only *Use recording* reaches `addFiles`, and the waveform is clicked to scrub. The playhead is scaled by the recorder's **own clock**, not by `player.duration` — a webm straight out of `MediaRecorder` carries no duration. Chrome answers `Infinity` for one until it has been seeked once, which leaves the preview unable to scrub or play to the end, so `loadedmetadata` seeks far past any real recording and puts the position back before anyone sees it.

**Closing the dialog gives the microphone back.** `upl._recordCleanup`, called from the dialog's own cleanup, stops the recorder, stops the tracks, **closes the AudioContext** (left open, the browser goes on showing the tab as using the microphone after the tracks stop) and revokes the preview's object URL. `recorder.start(1000)` takes a timeslice rather than one blob at the end: a long recording held whole in memory is a tab that dies before it can be uploaded.

`tests/test_upload_recording.py` reads the script itself — a broken string literal, a lost `addFiles`, a placeholder nothing fills in, or a waveform that decodes the recording are failures a browser shows and nothing else does.

## Settings

`utils/settings.py` `Settings` (pydantic `BaseSettings`, loaded from `.env`):

- `API_URL`, `OIDC_APP_LOGIN_ROUTE`, `OIDC_APP_LOGOUT_ROUTE`, `OIDC_APP_REFRESH_ROUTE`
- `STORAGE_SECRET` — keys AES-256-GCM encryption for browser storage
- Branding: `LOGO_*`, `FAVICON`, `TAB_TITLE`, `TOPBAR_TEXT`, `LANDING_TEXT`, `MANUAL_URL`
- `WHISPER_MODELS`, `WHISPER_LANGUAGES` (includes "Northern Sámi (Experimental)")
- Inference: `INFERENCE_ENABLED`, `INFERENCE_URL` (the hub is a separate app from the API — empty means they share a name, which is true behind the proxy and false in development, where the API answers 404 for anything inference asks it), `INFERENCE_WS_URL` (empty derives it from `INFERENCE_URL`), `INFERENCE_TIMEOUT`
- Editor: `NEW_CAPTION_SECONDS` (how long "Add caption after" makes a new caption, clamped to the room before the next one), `CHARACTER_LIMIT` (must match `SUBTITLE_LINE_LENGTH` in transcribe-worker — the worker wraps at it, the editor flags lines that exceed it), `CHARACTER_LIMIT_EXCEEDED_COLOR`, `MAX_SUBTITLE_LINES`, `MIN_CAPTION_SECONDS` (shortest a caption should be on screen; "Validate" warns below it), `CONFIDENCE_LOW`, `CONFIDENCE_MEDIUM`

Tunables belong here; wire-format constants do not. `WORDS_FORMAT_VERSION` in `utils/srt.py` stays in code — a deployment claiming a version the code does not implement would only mis-parse silently.

Access via `get_settings()` (cached).

## Admin hierarchy

- **BOFH** (`bofh=True`): sees everything, manages all realms and onboarding attributes
- **Realm Admin** (`admin=True`): scoped to own realm + `admin_domains`. Can manage rules for their realms
- **Regular User**: no admin access

## Onboarding rules (pages/admin/rules.py)

- Rules page at `/admin/rules` — table with create/edit/delete dialogs and per-row test/delete actions
- Realm field is a multi-select dropdown filtered to real domains (containing a dot/TLD)
- Rules scoped by realm — admins within the same organisation see the same rules
- BOFH users see all rules across all realms
- Onboarding attributes section hidden for non-BOFH users
- Help dialog explains rule matching, actions, scoping, and manual override
- "Attributes" terminology used throughout (not "JWT claims")

## Testing

```bash
# All tests
uv run pytest
```

A `pytest.ini` (gitignored, not committed) normally configures `asyncio_mode = auto`, `main_file = main.py`, and loads `nicegui.testing.user_plugin` — check it exists locally if async tests misbehave; the suite currently collects and passes without one too. Current test files: `tests/test_srt.py`, `tests/test_delete_range.py`, `tests/test_validation.py`, `tests/test_speech_timeline.py`, `tests/test_session.py`, `tests/test_storage.py`, `tests/test_transcript_editor.py`, `tests/test_my_edits.py`, `tests/test_words.py`, `tests/test_word_moves.py`, `tests/test_parse_txt.py`, `tests/test_caption_editor_styles.py`, `tests/test_billing_export.py`, `tests/test_event_loop_hygiene.py`, `tests/test_inference.py`, `tests/test_docx_export.py`, `tests/test_latex_export.py`, `tests/test_upload_recording.py`, `tests/test_mobile_layout.py`, `tests/test_review_assistant.py`, `tests/conftest.py`.
